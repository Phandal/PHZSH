alias ls="exa -a --icons --group-directories-first"
alias grep="grep --color=auto"

alias cp="cp -i"
alias rm="rm -i"
alias mv="mv -i"


if ! command -v bat &> /dev/null
then
  alias cat="batcat"
else
  alias cat="bat"
fi

alias n="nvim"
